@model IEnumerable<Service>

@{
    ViewData["Title"] = "Services";
}

<div class="page-header">
    <div>
        <p class="text-muted mb-0">Configure your service offerings and hourly rates</p>
    </div>
    <button type="button" class="btn btn-primary" onclick="showGlobalModal('@Url.Action("Create")', 'Add New Service')">
        <i class="bi bi-plus-lg me-1"></i> Add Service
    </button>
</div>

<div class="card p-0">
    <div class="card-body p-0">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th>Service Name</th>
                    <th>Hourly Rate</th>
                    <th width="140">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Any())
                {
                    @foreach (var svc in Model)
                    {
                        <tr>
                            <td class="fw-semibold">@svc.Name</td>
                            <td>₱@svc.HourlyRate.ToString("N2") / hr</td>
                            <td>
                                <button type="button" class="btn btn-sm btn-outline-warning" onclick="openEditModal(@svc.Id)" title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </button>
                                <button type="button" class="btn btn-sm btn-outline-danger" onclick="openDeleteModal(@svc.Id, '@svc.Name')" title="Delete">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="3" class="text-center text-muted py-4">
                            <i class="bi bi-gear fs-1 d-block mb-2"></i>
                            No services yet. <button type="button" class="btn btn-link p-0" onclick="showGlobalModal('@Url.Action("Create")', 'Add New Service')">Add your first service</button>.
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title"><i class="bi bi-exclamation-triangle me-2"></i>Confirm Delete</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete service <strong id="deleteItemName"></strong>?</p>
                <p class="text-danger small mb-0">This action cannot be undone.</p>
            </div>
            <div class="modal-footer">
                <form id="deleteForm" method="post" asp-action="Delete">
                    <input type="hidden" name="id" id="deleteId" />
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-danger">Delete Service</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        const deleteModal = new bootstrap.Modal(document.getElementById('deleteModal'));

        function openEditModal(id) {
            showGlobalModal('@Url.Action("Edit")/' + id, 'Edit Service');
        }

        function openDeleteModal(id, name) {
            document.getElementById('deleteId').value = id;
            document.getElementById('deleteItemName').innerText = name;
            deleteModal.show();
        }
    </script>
}
